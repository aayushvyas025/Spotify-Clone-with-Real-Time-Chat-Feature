import type {PlayerStoreInterface } from "@/types/interfaces/store/storeInterface";
import { create } from "zustand";

const usePlayerStore = create<PlayerStoreInterface>((set) => ({
    
})); 

export default usePlayerStore; 
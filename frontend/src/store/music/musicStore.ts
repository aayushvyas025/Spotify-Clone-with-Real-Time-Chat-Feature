import {create} from "zustand"


const useMusicStore = create((set) => ({
 albums:[],
 songs:[],
 fetchAlbums: async()=> {
    try {
        
    } catch (error:any) {
        
    }
 }
})); 

export default useMusicStore; 